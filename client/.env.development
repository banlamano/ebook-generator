# For local development, point to the backend server
REACT_APP_API_URL=http://localhost:4000/api

# Fix for webpack-dev-server compatibility
DANGEROUSLY_DISABLE_HOST_CHECK=true

# React dev server port
PORT=3000
